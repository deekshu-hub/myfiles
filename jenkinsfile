pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                sh 'docker build -t prt-ci-image .'
            }
        }

        stage('Run Container') {
            steps {
                sh 'docker run --rm prt-ci-image'
            }
        }
    }
}
